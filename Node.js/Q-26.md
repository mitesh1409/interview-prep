# How do you handle errors in asynchronous code? Provide examples.

```javascript
async function fetchData() {
    try {
        const response = await fetch("https://api.example.com/data");
        const data = await response.json();
        return data;
    } catch (error) {
        console.error("Error fetching data:", error);
        return null;
    }
}

fetchData();
```

---

**Todo**  

Take an example of booking an Uber ride:  

* Code this using callback, handle errors
* Code this using promise, handle errors
* Code this using async-await, handle errors
